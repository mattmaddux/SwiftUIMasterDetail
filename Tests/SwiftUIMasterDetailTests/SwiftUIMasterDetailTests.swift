import XCTest
@testable import SwiftUIMasterDetail

final class SwiftUIMasterDetailTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIMasterDetail().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
